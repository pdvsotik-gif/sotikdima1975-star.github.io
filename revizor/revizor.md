# Отчёт Revizor — 2026-03-31

## 🔴 Критические ошибки
- [ ] **Битые пути к ресурсам** — в файле `/pages/games/forza/motorsport8/tracks/mugello/index.html` используются некорректные пути `../../../../../css/header.css` и `../../../../../js/header.js`, которые не соответствуют реальной структуре проекта. Исправить на `/css/header.css` и `/js/header.js`.
- [ ] **Отсутствующие директории** — в проекте отсутствуют директории `/_sdk/` и `/css/`, на которые есть ссылки в HTML-файлах. Необходимо создать их и добавить соответствующие файлы.
- [ ] **Отсутствует favicon** — на сайте не найден favicon. Рекомендуется добавить `<link rel="icon" href="/images/favicon.ico" type="image/x-icon">` в `<head>`.
- [ ] **Отсутствует инициализация SDK** — в коде используется `elementSdk` и `dataSdk`, но SDK не инициализирован. Необходимо добавить скрипт инициализации или удалить ссылки.

## 🟡 Предупреждения
- [ ] **Tailwind CSS подключён через CDN** — используется `https://cdn.tailwindcss.com/3.4.17`, что небезопасно для продакшена. Рекомендуется использовать локальную сборку.
- [ ] **Lucide подключён через CDN** — аналогично, лучше использовать локальную версию для повышения производительности и безопасности.
- [ ] **Отсутствуют мета-теги Open Graph и Twitter Cards** — необходимо добавить OG-теги для улучшения SEO и отображения ссылок в соцсетях.
- [ ] **Использование `!important` в стилях** — в Tailwind при использовании CDN могут возникать проблемы с приоритетом стилей. Рекомендуется избегать `!important`.

## 🟢 Рекомендации по улучшению
- [ ] **Оптимизация изображений** — изображения в `/images/` можно оптимизировать (WebP, сжатие) для ускорения загрузки.
- [ ] **Lazy loading для изображений** — добавить `loading="lazy"` для всех `<img>`.
- [ ] **Добавить ARIA-атрибуты** — для кнопок табов использовать `role="tab"`, `aria-selected`, `aria-controls`.
- [ ] **Добавить Schema.org разметку** — для улучшения SEO и отображения в поиске.
- [ ] **Проверка на XSS** — убедиться, что динамический контент экранируется.

## ✅ Вывод
В первую очередь необходимо исправить пути к CSS/JS, создать отсутствующие директории и добавить favicon. Затем — перейти с CDN на локальные сборки Tailwind и Lucide. После этого — добавить мета-теги и улучшить доступность.